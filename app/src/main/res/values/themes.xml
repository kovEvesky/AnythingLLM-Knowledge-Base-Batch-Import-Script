<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 基础主题:ComponentActivity + Compose,深浅色由 Compose 侧 AnyllLLMTheme 控制 -->
    <style name="Theme.AnythingLLM" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 分享接收器:透明、无预览,接收后立即结束不闪屏 -->
    <style name="Theme.AnythingLLM.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>

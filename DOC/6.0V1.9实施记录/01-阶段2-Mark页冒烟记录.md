# V1.9 阶段 2 · Mark 流式收件箱模拟器冒烟记录

日期:2026-09-14
环境:AVD anythingllm_api36(API 36, 1080x2400), Debug 包签名安装(先卸载旧 release 包解决签名冲突)
结论:**阶段 2 核心交互全部通过**,可进入阶段 3。

## 验证项与结果

| # | 场景 | 操作 | 结果 |
|---|---|---|---|
| 1 | 四 Tab 骨架 | 安装启动 | ✅ Mark(M 文字)/To(T)/Sync(循环)/Settings(齿轮),占位页正常 |
| 2 | 空态 | 新装无数据 | ✅ "收藏流为空"提示 + Inbox 图标 |
| 3 | 分享收流 | `am start` ACTION_SEND 模拟分享链接 | ✅ 白卡出现在"待整理/今天"分组,标题抓取回填域名占位 |
| 4 | 副行信息 | 查看卡片 | ✅ "链接·未知来源·刚刚"(adb 模拟无 label,兜底生效) |
| 5 | 点击白卡 → 气泡 | tap 卡片 | ✅ 弹出"选择要归入的收藏夹"+ 工作/学习/积累 + 新建… |
| 6 | 气泡选夹 → 灰卡 | tap "工作" | ✅ 条目移入"已整理",右上角"已归入 工作",Snackbar"已归入「工作」" |
| 7 | 灰卡左滑撤销 | swipe 左 | ✅ 恢复白卡回"待整理",Snackbar"已撤销标记,内容已恢复" |
| 8 | 白卡左滑删除 | swipe 左 | ✅ 移入"已归入 回收站",Snackbar"已移到回收站收藏夹" |
| 9 | 白卡右滑归类 | swipe 右 | ✅ 弹出收藏夹气泡(两段式) |

## 迁移日志

- 新装环境 `MarkToMigrate: migrated=0 created=[] fallback=0`(无旧数据,正常)。

## 注意点(后续阶段处理)

1. **首启引导文案仍为 v1.7 旧稿**("收集箱/服务器文件夹/资料库")→ 阶段 3 更新为收藏夹体系文案
   (① 分享即流式 ② 滑一下即归类 ③ 一键同步)。
2. 测试中一次 `adb input swipe`(起点 x=800 接近卡片右缘)出现页面切到 To 的时序竞态,
   受控坐标重测全部通过;真机手势与 adb 注入手势行为可能不同,阶段 5 需真机复核。
3. 通知权限弹窗首次出现需手动允许(系统行为)。

## 截图

- `v19_mark_home.png` 四 Tab + 空态
- `v19_mark_shared.png` 白卡 + 首启引导(旧文案)
- `v19_bubble.png` 收藏夹气泡
- `v19_gray.png` 灰卡 + 已归入标签 + Snackbar
- `v19_undo2.png` 灰卡撤销
- `v19_del.png` 白卡左滑删除
- `v19_rswipe.png` 右滑弹气泡
